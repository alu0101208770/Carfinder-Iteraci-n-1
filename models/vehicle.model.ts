export class Vehicle {
  constructor (private id: number, private name: string, private tipo: string,
    private characteristics: string, private price: number, private stock: number,
    private model: string) {}
}